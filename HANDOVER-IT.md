# 交接说明 · 迁移到公司内部环境

给接手的 IT / 平台团队。目标是把这个站点从当前的「GitHub Pages + Supabase」形态迁到公司
统一环境。本文说清三件事：**它是什么、迁移要做什么、迁移过程中什么绝对不能破坏**。

---

## 1. 它是什么

DiDi Alianza 墨西哥 Vendor（配送商）等级体系的自助查询站点。

- **对商（Vendor）**：登录后只能看到自己一家的等级、六项指标得分、改进建议与收益预估
- **对 RM**：看名下商户全盘 + 单商详情 + S 级标杆
- **对内部运营 / 主管理员**：全部 102 家；主管理员另有规则试算与分数线沙盘

三语（中 / 西 / 英）。当前用户量：内部 7 个账号；**Vendor 正式账号尚未发放**（约 102 家 + 13 位 RM 待开）。

### 数据规模（很小）

| 表 | 行数量级 |
|---|---|
| 每期评分 `vg_vendor_scores` | 约 100 行/期，目前 2 期 |
| 商档案 `vg_vendor_profile` | 102 |
| 分布与等级聚合 | 每期数十行 |
| 用户与角色 | 目前 12 |

**没有高并发、没有大数据量、没有实时计算。** 页面读的是导入时预计算好的结果。

---

## 2. 当前架构

```
浏览器（静态页面，零构建）
   │  只持 Publishable key（公开、无任何权限）
   ▼
Supabase PostgREST  ──►  PostgreSQL
   │                        └─ 行级安全（RLS）：每张表逐条策略，按登录身份过滤行
   ▼
Supabase Auth（邮箱 + 密码）
```

**关键点：隔离完全由数据库的行级安全（RLS）强制。** 前端也做过滤，但那只是显示效果，
不是安全机制。一个 Vendor 即使手动构造请求，数据库也只会返回他自己那一行。

### 数据流

```
xlsx 源数据 ──(scripts/*.py 一次性提取)──► data/*.json（本地，不入库、不上传）
                                              │
                                    scripts/supabase_import.js
                                    （含排名与分布预计算）
                                              ▼
                                        Supabase 9 张表
                                              ▼
                                        浏览器按身份读取
```

规则（权重、档位、分数线、单价、红线参数）全部在 `data/rules.json`，导入时拆成两份进库：

- `vg_rules_public` —— 公示部分，所有登录用户可读
- `vg_rules_internal` —— 内部部分（备选分数线、SA 占比目标），仅主管理员可读

---

## 3. 迁移到公司环境要做什么

### 3.1 前端（最简单）

零依赖、零构建的静态站点，**任何能提供静态文件的服务都可以**：公司 CDN、Nginx、
内网静态托管、对象存储都行。

- 发布内容只需 `index.html` + `src/`（26 个文件）
- 用 hash 路由（`#/overview`），**不需要服务端 rewrite 规则**
- 全部引用为相对路径，可以挂在任意子路径下（如 `https://内网域名/vendor-level/`）
- 唯一需要改的配置是 `src/config.js` 里的后端地址与公开密钥

### 3.2 后端与数据库（关键）

现在用的是 Supabase。迁到公司环境有两条路：

**路线 A：公司自建 PostgreSQL + PostgREST + 一套认证**
改动最小。`supabase/migration_001_schema_rls.sql` 里的建表与 RLS 策略是标准 PostgreSQL，
可直接执行。需要替换的只有认证部分：策略里用到 `auth.uid()`（当前登录用户 id）和
`vg_my_role()`（读该用户的角色），换成公司认证体系提供的等价物即可。

**路线 B：公司现有应用平台（自研后端 / 中间层）**
这时前端的 `src/data.js` 是唯一需要重写的文件——它是数据接入层，页面代码不感知数据来源。
只要新实现返回相同的数据形状（见文件顶部注释），页面零改动。

> ⚠️ **走路线 B 时必须自己实现等价的行级隔离。** 现在这套安全性完全来自 RLS；
> 如果新后端只是「查全量再在应用层过滤」，一旦某个接口漏了过滤条件就是全量泄漏。
> 建议：无论后端怎么写，数据库层的 RLS 都保留着当第二道防线。

### 3.3 认证

当前是 Supabase Auth（邮箱 + 密码，账号由主管理员分配，无自助注册、无自助改密）。
迁到公司环境后**建议直接接公司 SSO**——Vendor 是外部合作伙伴，可能需要单独的外部身份源，
这点需要你们评估。

用户与角色的映射表是 `vg_user_roles`：

| 字段 | 说明 |
|---|---|
| `user_id` | 认证系统的用户 id |
| `role` | `vendor` / `rm` / `ops` / `admin` |
| `vendor_code` | 仅 `vendor` 角色：绑定的商编码（完整全串） |
| `rm_name` | 仅 `rm` 角色：绑定的 RM 姓名 |

### 3.4 需要你们提供的

1. 静态托管位置（内网域名 / 路径）
2. 数据库实例（PostgreSQL 13+）
3. 认证方案（SSO 或独立账号体系），以及 Vendor 这类外部用户怎么接
4. 定时任务的位置（每月/每周数据导入，见 3.5）

### 3.5 数据更新流程（迁移后要接的）

现在是人工触发的本地脚本：

```bash
python scripts/build_vendor_profile.py   # xlsx → 商档案
bun scripts/build_vendors.js             # 原始指标值 → 评级结果
bun scripts/build_periods_index.js       # 期次索引
bun scripts/supabase_import.js 2026-08   # 导入指定期（含排名与分布预计算）
```

建议迁移后改成每月一次的定时任务。**注意 `supabase_import.js` 用的是具有完整权限的
Secret key，只能在服务端运行，绝不能进前端。**

---

## 4. 迁移中绝对不能破坏的不变量

这几条是这个系统正确性的地基。迁移时如果只保留一页说明，请保留这一页。

### ① 隔离必须在数据层，不能只在应用层

Vendor 只能看到自己那一行，RM 只能看到「名下 ∪ S 级」。当前由 RLS 保证。
`scripts/test_rls.js` 是验收脚本，**每次改表或改策略后必跑**，其中最重要的一项是：
用未登录状态（仅公开密钥）直接请求 9 张表，断言全部返回 0 行。

> RLS 配错时页面看起来完全正常（因为前端也在过滤），只有这个脚本能发现。

### ② 规则是数据，不是代码

`data/rules.json` 是等级规则的唯一事实来源。**任何在代码里写死权重、阈值、分数线、
单价的做法都是 bug。** 分数线会随市场定期校准，写死会导致代码与公示规则分叉。

改规则的正规流程是：改 `rules.json` → 追加 changelog → 跑回归 → 重新导入。
`scripts/validate_rules.js` 会校验「changelog 声称改了的东西是否真的改了」。

### ③ 引擎只能有一份

`src/engine/` 同时被浏览器、校验脚本和单元测试引用。**不要为了后端方便再实现一份计分逻辑**
——一旦有两份，迟早会出现「页面显示 A、结算按 B」。它是零依赖的纯 JS，可以直接搬到服务端。

### ④ 派生值实时算，不读存储标记

诸如「是否被规模门槛拦截」这类布尔标记，一律由引擎按当前规则现算。
历史上源数据的备注列出现过跨版本继承污染，导致商家被错误标记。存储值只用于交叉校验。

### ⑤ 数据对不上就报错，不要兜底

缺失的指标值不能当 0（那会让数据缺口变成「看起来正常的低分」）；缺失的首单日期不能默认为
「已过保护期」（那会让新商收到错误的清退警告）。现有脚本都会显式报告并中止，请保留这个行为。

### ⑥ 发布面最小化

站点 URL 是公开可访问的。**只发布 `index.html` + `src/`**，脚本、迁移 SQL、测试、
内部文档一律不发布。`scripts/publish_site.sh` 内置自查，产物里出现内部文件或 Secret key 会中止。

### ⑦ 密钥分级

- **Publishable / anon key**：设计上就是公开的，随页面下发，本身没有任何权限，
  能读到什么完全由 RLS 决定 → 可以进前端、进仓库
- **Secret / service_role key**：绕过 RLS，等同于裸库 → **只能在服务端**，
  绝不能进前端、配置文件或任何提交

---

## 5. 验收（迁移完成后请跑一遍）

```bash
bun scripts/validate_rules.js   # 规则文件自洽
bun scripts/recalc.js           # 逐商比对，须 102/102 全对
bun test                        # 引擎与保护期单元测试
bun scripts/test_rls.js         # 越权测试（最重要）
bun scripts/test_roles.js       # 四角色实际可见范围
bun scripts/render_check.js     # 全站渲染自检（每期 × 每商 × 每页 × 三语）
```

迁移后的额外检查：

- [ ] 用未登录状态直接请求所有表 → 全部 0 行
- [ ] 用 Vendor A 的账号请求 Vendor B 的数据 → 返回为空
- [ ] RM 账号看不到名下之外的非 S 级商
- [ ] 页面源码里没有任何 Secret key（只有设计上公开的 Publishable key）
- [ ] 三种语言都能正常显示，无中文残留

---

## 6. 交接清单

| 项 | 位置 | 备注 |
|---|---|---|
| 源码 | 本仓库 | 零依赖、零构建 |
| 数据库结构与 RLS 策略 | `supabase/migration_*.sql` | 标准 PostgreSQL，可直接执行 |
| 数据导入管道 | `scripts/supabase_import.js` | 含排名与分布预计算 |
| 回归套件 | `scripts/` 下六个脚本 | 见第 5 节 |
| 业务数据 | **不在仓库内** | 含商家评级与得分，由业务侧单独提供 |
| 密钥 | **不在仓库内** | 模板见 `scripts/.supabase_secrets.template.json` |
| 规则与口径的业务背景 | 业务侧文档 | 分数线为什么这么定、指标口径的历史演变 |

有任何关于计分口径、规则演变或数据来源的问题，请联系业务侧负责人，不要自行推断——
指标口径改过多次，代码注释里记录了每次改动的原因。
